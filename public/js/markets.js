let markets = $('#table_id').DataTable();
let assessmentArray = new Array();

$(document).ready( function () {
  $('#table_id').DataTable();

  /*$.get('/marketss', function(rows){

   
    var currentObj;

    for (var i = 0; i < rows.length; i++){
		currentObj = JSON.parse(rows[i].name);
		console.log(currentObj);
		//assessmentArray[i] = currentObj;
		assessmentArray.push(currentObj);
	}

	console.log("assessmentArray is " + assessmentArray);
	});*/
});